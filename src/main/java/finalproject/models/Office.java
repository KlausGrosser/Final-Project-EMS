package finalproject.models;

//This is an enum class which represents a group of constants (unchangeable variables)
public enum Office {
  BERLIN,
  PUEBLA,
  ROSARIO
}

// I listed 3 cities form different countries in reason to use the currency API later
